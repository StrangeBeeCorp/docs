!!! tip "Variables"
    Insert a variable with the **$var** button, or type `$` and select a variable from the suggestions. To access a field, select the variable to edit its jq expression, or use the **+** button to insert the variable and the field together.
