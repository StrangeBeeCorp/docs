Extra headers added to every request sent by the node. Select :fontawesome-solid-plus: to add a header key and its value.

!!! warning "Header values are stored in plaintext"
    Header values appear in plaintext in the workflow execution history. Reference a [secret global variable](/flow/user-guides/manage-global-variables/) instead of pasting a credential directly.
