!!! warning "Required permissions for managing case and alert statuses"
    Only users with an admin-type profile that has the `managePlatform` permission can manage case and alert statuses in TheHive.